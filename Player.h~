#ifndef PLAYER_H
#define PLAYER_H

#include <iostream>
#include <cstring>
#include <vector>
#include "Room.h"

using namespace std;

class Player{
 public:
  Player(Room*);
  char* getName();
  Room* getCurrentRoom();
  vector<Item*>* getItems();
  void setCurrentRoom(Room*);
  Item* takeItem(char*);
  void addItem(Item*);
  void printInventory();
  void checkWin();
  bool won(char*);
private:
  char name[20];
  vector<Item*> items;
  Room* currentRoom;
  Room* newRoom;
};

#endif
